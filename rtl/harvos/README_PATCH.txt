HarvOS patched RTL (decoder, harvos_core, dcache_line, csr_file, etc.)

Drop the 'rtl/harvos' folder from this zip into your project,
replacing the existing folder. This set contains the working sources
that remove '...' placeholders and match the whitepaper's decode and LSU/CSR behavior.